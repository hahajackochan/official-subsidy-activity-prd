{
  "name": "official-subsidy-activity-prd",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:pages": "node scripts/build-pages.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "npm test && npm run build:pages"
  }
}
